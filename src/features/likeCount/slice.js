// import { createSelector, createSlice } from "@reduxjs/toolkit";

// const initialState = {
//   hearts: [],
// };

// const reducers = {
//   toggleHeart: (state, action) => {
  
    
//   },
// };

// const name = "heart";
// export const heartSlice = createSlice({
//   name,
//   initialState,
//   reducers,
// });

// const selectHeartState = createSelector((state) => state.hearts);

// export const user = heartSlice.name;
// export const userReducer = userSlice.reducer;
// export const userAction = userSlice.actions;

// export const userSelector = {
//   selectUser: (state) => {
//     console.log("state", state);
//     return selectUserState(state[user]);
//   },
// };
